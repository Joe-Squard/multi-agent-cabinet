# クイックスタートガイド

## 🚀 5分で始める

### 1. セットアップ（初回のみ）

```bash
cd /path/to/multi-agent-cabinet
./first_setup.sh
```

### 2. システム起動

```bash
./cabinet_start.sh
```

以下の3つのtmuxセッションが起動します：
- `pm`: 首相
- `chief`: 内閣官房長官
- `bureau`: 官僚×8

### 3. 首相に接続

```bash
tmux attach-session -t pm
```

### 4. タスクを実行してみる

首相セッション内で、天皇（あなた）として命令を出します：

```
「Reactの最新バージョンと前バージョンの主な差分を調査せよ」
```

首相が内閣官房長官にタスクを委譲し、官僚が並列で実行します。

### 5. 進捗確認

別のターミナルで：

```bash
cat dashboard.md
```

または、内閣官房長官セッションに接続：

```bash
tmux attach-session -t chief
```

### 6. 官僚の様子を見る

```bash
tmux attach-session -t bureau
```

8つのペインが並んでおり、各官僚が並列で作業している様子が見えます。

### 7. セッション間の移動

- **デタッチ**: `Ctrl+b` → `d`
- **再接続**: `tmux attach-session -t <session_name>`

### 8. システム停止

```bash
./cabinet_stop.sh
```

---

## 📝 tmux基本操作

| 操作 | キー |
|---|---|
| セッションをデタッチ | `Ctrl+b` → `d` |
| ペイン間移動 | `Ctrl+b` → 矢印キー |
| ペインのスクロール | `Ctrl+b` → `[` (qで終了) |
| ウィンドウ一覧 | `Ctrl+b` → `w` |
| セッション一覧 | `tmux list-sessions` |

---

## 🎯 実際の使用例

### 例1: 複数フレームワークの比較調査

**天皇（あなた）→ 首相**:
```
「Next.js、Remix、Astro、Nuxt、SvelteKitの5つのフレームワークについて、
それぞれの特徴、パフォーマンス、エコシステムを調査して比較表を作成せよ」
```

**首相 → 内閣官房長官**:
```bash
./scripts/inbox_write.sh chief "
task_id: task_001
title: JSフレームワーク比較調査
description: Next.js、Remix、Astro、Nuxt、SvelteKitの比較
priority: high
frameworks:
  - Next.js
  - Remix
  - Astro
  - Nuxt
  - SvelteKit
"
```

**内閣官房長官 → 官僚（並列実行）**:
```bash
./scripts/inbox_write.sh bureau_1 "task: Next.js調査"
./scripts/inbox_write.sh bureau_2 "task: Remix調査"
./scripts/inbox_write.sh bureau_3 "task: Astro調査"
./scripts/inbox_write.sh bureau_4 "task: Nuxt調査"
./scripts/inbox_write.sh bureau_5 "task: SvelteKit調査"
```

5つのタスクが同時並行で実行され、数分で完了。

---

### 例2: コンポーネント群の実装

**天皇（あなた）→ 首相**:
```
「Reactでダッシュボード用のHeader、Sidebar、Footer、Card、Tableコンポーネントを作成せよ。
TypeScript + Tailwind CSSを使用すること」
```

**首相 → 内閣官房長官**:
```bash
./scripts/inbox_write.sh chief "
task_id: task_002
title: ダッシュボードコンポーネント実装
description: Header、Sidebar、Footer、Card、Tableコンポーネント作成
tech_stack:
  - React
  - TypeScript
  - Tailwind CSS
priority: high
"
```

**内閣官房長官 → 官僚（並列実行）**:
```bash
./scripts/inbox_write.sh bureau_1 "component: Header"
./scripts/inbox_write.sh bureau_2 "component: Sidebar"
./scripts/inbox_write.sh bureau_3 "component: Footer"
./scripts/inbox_write.sh bureau_4 "component: Card"
./scripts/inbox_write.sh bureau_5 "component: Table"
```

5つのコンポーネントが同時に実装され、統合される。

---

## 🔍 トラブルシューティング

### セッションが見つからない

```bash
# セッション一覧確認
tmux list-sessions

# セッションがない場合は起動
./cabinet_start.sh
```

### メッセージが届かない

```bash
# inbox の内容を確認
ls -la queue/inbox/

# 手動でメッセージ送信テスト
./scripts/inbox_write.sh pm "test message"
cat queue/inbox/pm.yaml
```

### 官僚が動かない

```bash
# tmux セッション内でプロセス確認
ps aux | grep inbox_watcher

# inbox_watcher を再起動（官僚セッション内で）
./scripts/inbox_watcher.sh bureau_1 &
```

---

## 📚 次のステップ

1. **詳細ドキュメントを読む**
   - `README.md`: システム全体概要
   - `CLAUDE.md`: システム指令
   - `instructions/`: 各エージェントの役割

2. **カスタマイズ**
   - `config/settings.yaml`: システム設定
   - `config/agents.yaml`: エージェント定義

3. **拡張**
   - 専門大臣の追加
   - Tool統合
   - ntfy連携（スマホから操作）

---

## 💡 ヒント

- **並列化のコツ**: 独立したタスクは必ず並列実行
- **明確な指示**: 官僚への指示は具体的に
- **進捗確認**: dashboard.mdをこまめにチェック
- **デバッグ**: tmuxセッションで直接観察

---

**楽しんでください！** 🎉
